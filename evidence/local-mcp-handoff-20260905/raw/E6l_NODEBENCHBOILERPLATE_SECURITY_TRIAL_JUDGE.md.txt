# Boilerplate nine-row dependency trial: independent judgment

Builder. **Approve the exact external lockfile proposal for a separately bound transfer.** The developer's existing local MCP setup still starts, handles protocol failures and preserves workflow records with the patched dependencies. This is a bounded dependency repair; the remaining Sharp advisory prevents a security-complete handoff.

**Re your request:** make each repository usable and ready for developer or coding-agent handoff using planner–worker–judge — this review independently verifies the proposed dependency delta, retained failure evidence, local compatibility and durable records before it reaches the repository.

Reviewed wrapper: `4e5620f40ac3c3eb25bf3e2d9205b1d2ef929bff719f7cf760645743efe6d36e`. Proposed `package-lock.json`: `446e1c6e93405884c418fa594fd0092e92e24451d1a90341d72c18a88a7ec15c`. The external 15-file source digest is `e206dc63ebb1198bd6ca15a4bd6030603f340b591d969986fd694fce69b8638f`; its only difference from the accepted candidate digest `193ba3feda6209e843dce8c20ed4ff456b95ac1f1d8787b3479508975997dad4` is this lockfile. The candidate and original checkout remain untouched. The reviewer is a reused nonauthor who previously judged the entrypoint repair, not the trial's implementer or original recorder.

The lock changes exactly nine existing rows and leaves the other 350 rows intact, with no added or removed package paths. `package.json`, the root lock entry and other top-level lock properties remain exact. The temporary explicit resolver constraints were removed before normal installation. The changes are @hono/node-server 1.19.15, TOON 2.3.1, xmldom 0.8.15, fast-uri 3.1.6, Hono 4.12.34, ip-address 10.3.1, Nano ID 3.3.18, PostCSS 8.5.23 and qs 6.16.0. Version, registry resolution and integrity are the changed fields; PostCSS also legitimately advances its Nano ID declaration from `^3.3.12` to `^3.3.16`. All ten actual incoming declarations accept the selected versions. Declared Node engine ranges accept the project's Node 20.19/22.13 lower bounds; that is compatibility metadata, not native execution on those versions. [Exact readback](E6l-security-trial-independent-01/lock-readback.json), [range checks](E6l-security-trial-independent-01/resolution-compatibility.json)

All 695 original trial payloads, totaling 17,183,483 bytes, match the frozen manifest. The accepted candidate's 15 source files, 348 named installed files and Git state remain exact; the original primary's 13 files match its own baseline. All 684 loaded canary source bindings match the retained reports. All four generated starter outputs are identical between candidate and external trial. The normal installation, build, two existing tests, lint, compiled greeting, lint rejection/recovery and protocol command streams match their receipts. No completed full suite or installation was repeated by the judge.

The original trial's ten selected canary scenarios contain 71 passing patched assertions across eight families. The judge replayed only four finite mechanisms against both actual installed versions in new owned fixtures: TOON object prototype handling, URI parsing plus local AJV, XML serialization plus the real XLSX reader, and Hono static-path handling. The four old-library runs failed their expected assertions; the four patched runs passed all **48** assertions. Eight child processes used unchanged five-second budgets and closed without survivors. Every completed replay reports zero global fetch attempts. No listener, provider, OCR, image/model action, installation or application endpoint was used. [Independent replay](E6l-security-trial-independent-01/selected-canary-replay.json)

The claims stay at their measured layer. TOON shows a decoded object's prototype changing, not global prototype pollution. URI/IP tests do not certify an application's complete URL policy. The Windows static case rejects an encoded backslash to an owned same-root sentinel; it is not an outside-root disclosure exploit. The old Nano ID resource failures remain retained and were not repeated by the judge. Hono's finite CORS case passed both versions, so no denial-of-service reproduction or performance improvement is claimed. The XLSX observer correction changes only the expected all-sheet API shape and three phase markers; workbook/library inputs and the five-second deadline remain unchanged. The first wrong-shape failure and unattributed old-library timeout remain evidence.

The judge also independently reopened the owned workflow database with SQLite `mode=ro&immutable=1` after checking for a nonempty WAL. Its bytes remained exact. It contains 81 native tables, six rows bound to actual command outputs, eight distinct history entries with four pass/four fail values, and one learning row after eight updates. Returned IDs agree with actual raw tool replies; code review, the negative gate and release/flywheel gates remain false, and the failed phase remains failed. All 50 saved replies occur in the actual process stdout. Raw protocol responses independently confirm 20/273/567 starter/core/full tools, 14 prompts, matching restart inventories, error `-32601`, eight repeated lists and successful native reopening. The separate native-write probe counts 81 tables before creating its probe table and 82 on reopen; this is distinct from the 81-table workflow database. These tools record supplied evidence and do not confer human approval or execute the compiler themselves. [Database/readback bindings](E6l-security-trial-independent-01/raw-and-sqlite-readback.json), [raw protocol verification](E6l-security-trial-independent-01/protocol-raw-readback.json)

Reliability review preserves the original boundaries: protocol pending requests, frame/output limits and deadlines are unchanged; negative responses and false gates remain honest; no score or CAS policy changed. The size/loop and URI cases harden library behavior but do not establish all-tool collection bounds, body limits, URL validation or exception handling. No new application route or externally fetched response was introduced. These limits remain part of the accepted entrypoint scope, not silently promoted to a full backend certification.

The actual audit still exits **1**, with **two high entries**: Sharp `<0.35.0` and NodeBench MCP's inherited Sharp finding. MCP remains 3.2.1 and declares optional Sharp `^0.34.5`; the patched Sharp 0.35.0 is outside that contract. The trial neither removes the optional capability nor overrides that range. Presence is established, while exploitability through a particular application action was not tested. Keep the advisory open for an upstream-compatible package decision. [Retained audit](E6l-nodebenchboilerplate-security-trial-01/audit.stdout.txt)

No corrective source change is required for this trial. Root may prepare the exact lock-only transfer and a truthful current handoff, then separately bind transferred bytes and normal integration. Keep the original 11-entry evidence historical and disclose the new two-entry audit failure. Raw profile, environment, database and cache evidence is operator-local; publication needs an explicit finite selection. Linux/Node 20 native execution, Docker, global host activation, all full MCP tools, downstream app behavior and full visual/product grades remain unverified. The read-only guard is **closed unchanged**. [Final independent custody](E6l-security-trial-independent-01/guard-after.json)
