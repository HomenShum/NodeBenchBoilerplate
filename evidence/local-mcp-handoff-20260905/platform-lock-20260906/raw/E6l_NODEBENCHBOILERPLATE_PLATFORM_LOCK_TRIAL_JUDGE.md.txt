# Platform lock trial: accepted for guarded source transfer

Builder. A Linux developer could install this starter and compile its TypeScript, but the tests could not start because the lock omitted Rollup's Linux package. **APPROVE_EXACT_LOCK_TRANSFER_SCOPED** for lock SHA256 `ae1f98f63c6b7a1f33bc62931cfd61e621901a8d8c2f8e2862d63b09cd63233f`. The restored metadata and fresh Windows consumer are accepted; a new ordinary Linux CI run is still required before merge.

**Re your request:** make each repository ready for usage and handoff with independent judging — the platform dependency repair now has exact-version, registry and real local-consumer evidence.

The earlier first-attempt [shared failure](E6l_NODEBENCHBOILERPLATE_SHARED_JUDGE.md) remains unchanged. Node 20 failed; Node 22 was overall cancelled but its Test step also failed with the missing Rollup package. Lint and protocol were skipped on both. The failure applies to original source `5b0181dbd82f8261d93af6f69849124e3a069e9f`, tree `2e2a63198b3e4b196d0a54e9ef336374f71e820f`.

The final external resolution adds **59 package records** to the original359: 23 Rollup, 25 esbuild, nine canvas and two fsevents. **No existing package version or path is removed or changed.** Of the old rows,356 are object-identical; only the three incomplete parents gain npm-generated `resolved` and `integrity`. All nine previously reviewed security fixes and Sharp's complete platform records remain exact. Original package.json and root dependency edges are restored; there is no permanent direct dependency, override or workflow workaround. [Independent exact delta and guard](E6l-platform-lock-independent-01/report.json).

I independently compared all **59 new plus three rebuilt records** with exact public registry JSON: package/version, tarball, integrity, OS, CPU, engines and dependency maps agree. Every one of111 optional dependency addresses resolves in the resulting lock; the three affected parent ranges and fsevents placement are checked. Playwright retains its exact2.3.2 nested record while compatible consumers resolve root2.3.3. Registry libc declarations are retained as evidence; npm10 does not serialize them into every lock row, so no invented fields were added. This is metadata coverage, not execution on every OS/architecture. [Registry bindings](E6l-platform-lock-independent-01/registry-bindings.json), [edge addresses](E6l-platform-lock-independent-01/optional-edge-addresses.json).

The fourth attempt deliberately invalidated only the three incomplete parent rows in a disposable input, kept the other356 equal, temporarily constrained their existing versions, and let ordinary npm rebuild them. Original manifest bytes were restored and reconciled through npm. I checked the saved invalidated/intermediate/final inputs and helper. The preceding unchanged-lock, version-drifting fresh-lock and incomplete named-update attempts remain retained; none was promoted. This avoids pretending that ordinary unchanged-input regeneration worked. [Root plan](E6l-platform-lock-parent-rebuild-plan.md), [actual commands and proposal](E6l-platform-lock-resolution-04/receipt.json).

The actual fresh Windows consumer uses this exact lock and16 source files. Node executable metadata is22.22.2 and installed npm metadata10.9.7. Normal `npm ci`, build, test, lint and `verify:mcp` all exited0 with no recorded survivors. The real logs show **one Vitest file / two tests** and **25 passing protocol assertions**. Six raw inventories confirm starter20, core273, full567 tools, each with14 prompts; configured restart is covered. Invalid preset and missing local installation each exit1 as expected. No RPC tool invocation is made by this proof. Native SQLite3.49.2 uses the package-local adapter, writes a row with a changes1 assertion, and reopens/readbacks it in another process; reported tables are81 before the probe table and82 on reopen. [Raw consumer readback](E6l-platform-lock-independent-01/consumer-readback.json), [exact copied logs and protocol report](E6l-platform-lock-independent-01/consumer-copy-map.json).

I reopened all16 trial source and four compiled files, checked six actual inventories, and preserved18 exact consumer/protocol raw copies. No full test or protocol rerun was performed by the judge. The older50-call AGENTS journey remains historical, not relabeled as this run. The current install still reports **two high audit findings**; the previously identified Sharp/MCP issue remains held. The known prebuild-install deprecation and npm upgrade notice are retained, with no automatic upgrade.

Final guards independently confirm all302 current candidate files and13 original-primary files, raw indexes, refs, worktree registrations, HEAD and status unchanged. **Guard closed.** Root may transfer only the accepted lock plus separately reviewed truthful current handoff/binding metadata, retain all279 historical raw copies, commit normally, and obtain new ordinary Linux Node20/22 results. This judgment does not approve merge, certify the historical packet as current-lock execution, or claim provider, Docker, global-host, downstream, deployment or full-grade readiness.
